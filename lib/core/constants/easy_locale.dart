import 'dart:ui';

import 'package:top_jobs/core/constants/app_locale_keys.dart';

class EasyLocale {
  static const all = [
    const Locale(AppLocaleKeys.ru),
    const Locale(AppLocaleKeys.uz),
  ];
}
