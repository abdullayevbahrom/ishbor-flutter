E2E fixtures used by the Docker-based Flutter runner.

- `sample_image.png.b64`: base64-encoded 1x1 PNG for image picker flows.
- `sample_document.txt`: plain text document for file picker flows.
- `mock_location.json`: deterministic location fixture for map fallbacks.
